#include<stdio.h>
int main()
{
    int hours,miniutes;
    printf("enter the value of hours:\n");
    scanf("%d",&hours);
    miniutes=hours*60;
    printf("the value of miniutes is %d",miniutes);
    return 0;
}